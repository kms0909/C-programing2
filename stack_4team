1. 스택이란?
후입선출 (LIFO) 원칙에 따라 데이터를 저장하는 자료구조입니다.

2. 스택의 응용
주로 웹 브라우저의 전에 있던 페이지나 앞에 있던 페이지로 가기, 되돌리기 또는 앞으로가기 기능 등으로 사용됩니다.

3. 스택 구현
#include<stdio.h>

int sp = -1;
int v[1000];

void push(int i){
	if(sp == 999) {
		printf("Stack is Full!!\n");
	 	return;
	} 
	esle v[++sp] = i; 
}
int pop(){ 
	if(sp == -1){
		printf("Stack is Empty\n");
		return 0;
    	}
	return(v[sp--]); 
}
int main(){
	push(1);
	push(2);
	push(3);
	printf("%d\n", pop()); 
	printf("%d\n", pop());
	printf("%d\n", pop());
		
	pop(); // 언더플로우
		
	for(int i=0;i<=1000;i++) push(1); // 오버플로우
}
